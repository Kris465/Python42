const prompt = require('prompt-sync')();

// first

for (number_conor_first in range(1, 5)) {
    alert(`Введите координады ${number_conor} угла`)
    let x = Number(prompt('Введите координату x: '))
    let y = Number(prompt('Введите координату y: '))
}
// second

for (number_conor_second in range(1, 5)) {
    alert(`Введите координады ${number_conor} угла`)
    let x = Number(prompt('Введите координату x: '))
    let y = Number(prompt('Введите координату y: '))
}