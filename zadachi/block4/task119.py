a, b = map(int(input("Введите координаты фигуры: ")).split())
c, d = map(int(input("Введите координаты поля: ")).split())
piece = input("Введите тип фигуры(Ладья, Король, Ферзь, Слон, Конь, Белая пешка , Черная пешка): ")

m = {
    "Ладья": lambda x1, y1, x2, y2: x1 == x2 or y1 == y2,
    "король": lambda x1, y1, x2, y2: abs(x1 - x2) <= 1 and abs(y1 - x2) <= 1,
    "ферзь": lambda x1, y1, x2, y2:////
}
